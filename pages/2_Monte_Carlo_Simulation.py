"""Placeholder for CLAUDE.md build-order step 4 (Monte Carlo engine)."""

from __future__ import annotations

import streamlit as st

st.set_page_config(page_title="Monte Carlo Simulation", page_icon="🎲", layout="wide")

st.title("Monte Carlo Simulation")
st.caption("CLAUDE.md build-order step 4: NPV + Monte Carlo engine.")

st.info(
    "**Not built yet.** This page will host the Monte Carlo simulation engine once "
    "step 4 is underway. Nothing here is real data or a preview of results — it's a "
    "placeholder so the app's navigation shell is complete before that content "
    "exists."
)
