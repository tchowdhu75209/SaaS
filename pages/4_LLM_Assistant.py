"""Placeholder for CLAUDE.md build-order step 6 (multi-agent LLM assistant)."""

from __future__ import annotations

import streamlit as st

st.set_page_config(page_title="LLM Assistant", page_icon="🤖", layout="wide")

st.title("LLM Assistant")
st.caption(
    "CLAUDE.md build-order step 6: multi-agent LLM assistant (Simulation Agent + "
    "Data Analyst Agent + Orchestrator)."
)

st.info(
    "**Not built yet.** This page will host the multi-agent assistant from step 6 "
    "once that work is underway. Nothing here is a real conversation or a preview "
    "of behavior — it's a placeholder so the app's navigation shell is complete "
    "before that content exists."
)
