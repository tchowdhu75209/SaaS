"""Placeholder for CLAUDE.md build-order step 4 (metrics/judgment content)."""

from __future__ import annotations

import streamlit as st

st.set_page_config(page_title="Metrics & Judgment Calls", page_icon="📐", layout="wide")

st.title("Metrics & Judgment Calls")
st.caption("CLAUDE.md build-order step 4: metrics/judgment content, NPV engine.")

st.info(
    "**Not built yet.** This page will hold the derived metrics and the judgment "
    "calls behind them (e.g. NPV assumptions) once step 4 is underway. Nothing here "
    "is real data or a preview of results — it's a placeholder so the app's "
    "navigation shell is complete before that content exists."
)
