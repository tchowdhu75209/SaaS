"""Placeholder for CLAUDE.md build-order step 5 (real-data aggregate model +
labeled-synthetic classifier)."""

from __future__ import annotations

import streamlit as st

st.set_page_config(page_title="Predictive Models", page_icon="🔮", layout="wide")

st.title("Predictive Models")
st.caption(
    "CLAUDE.md build-order step 5: real-data aggregate model + labeled-synthetic "
    "classifier."
)

st.info(
    "**Not built yet.** This page will host the predictive models from step 5 once "
    "that work is underway. Nothing here is real data or a preview of results — "
    "it's a placeholder so the app's navigation shell is complete before that "
    "content exists."
)
